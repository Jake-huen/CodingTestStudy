package Algorany;

class DeliverGoldAndSilver {
    // 금 a, 은 b
    // i번 도시 -> g[i], s[i], 트럭 1대
    // i번 도시의 트럭은 오직 새 도시를 짓는 건설 장소와 i번 도시만을 왕복할 수 있으며, 편도로 이동하는 데 t[i]시간이 걸리고, 최대 w[i]kg 광물 운반 가능

    public long solution(int a, int b, int[] g, int[] s, int[] w, int[] t) {
        long answer = -1;

        return answer;
    }
}
